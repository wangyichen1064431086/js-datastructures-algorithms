/**
 * 
 * @param {Number} capacity  背包能装入的最大重量
 * @param {Array} weights 若干种物品的单件重量
 * @param {*} values 这些物品分别对应的单件价值
 * @param {*} n 物品种类数量，也即weights.length,也即values.length
 */
function knapSack(capacity, weights, values, n) {
  
}